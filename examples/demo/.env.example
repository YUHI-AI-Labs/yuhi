# Example env for the demo. Copy to `.env` (git-ignored) to see Yuhi route it
# "Exclude" via the block-env rule — a real .env is never committed or sent.
# Values below are harmless placeholders.
OPENAI_API_KEY=example-placeholder-not-a-real-key
DATABASE_URL=postgres://example:example@localhost:5432/appdb
