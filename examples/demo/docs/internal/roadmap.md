# Internal Roadmap

This internal document is routed **Prepare locally → Remove secrets** by the
`docs/internal/**` rule, so it never reaches the AI verbatim. It stands in for a
doc that would contain an internal access token and private plans — the contents
here are intentionally harmless so the public repo carries no real-looking secret.
